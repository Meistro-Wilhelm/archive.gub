# CLAUDE.md — AI Assistant Guide for archive.gub

## Project Overview

**archive.gub** is a website that houses the Güb Files.

- **Repository:** Meistro-Wilhelm/archive.gub
- **Author:** Meistro-Wilhelm (samuelgborn@gmail.com)
- **Status:** Early development — project skeleton only

## Repository Structure

```
archive.gub/
├── CLAUDE.md        # This file — AI assistant guide
└── README.md        # Project description
```

The project has no source code, dependencies, or build tooling configured yet. As the project grows, update this section to reflect the actual directory layout.

## Development Setup

No build system or package manager is configured. When one is added, document setup steps here:

```sh
# Example (update when a framework is chosen):
# npm install
# npm run dev
```

## Build & Run Commands

_None configured yet._ Update this section as scripts are added (e.g., in `package.json`).

## Testing

_No test framework configured._ When tests are added, document:

- How to run the full test suite
- How to run a single test file
- Any test naming conventions

## Linting & Formatting

_No linter or formatter configured._ When added, document commands here (e.g., `npm run lint`, `npm run format`).

## Code Style & Conventions

Since the project is in its initial phase, establish conventions early:

- Choose and document a code style guide when the first source files are added
- Configure an `.editorconfig`, linter, and formatter to enforce consistency
- Add a `.gitignore` appropriate for the chosen tech stack

## Git Workflow

- **Default branch:** `main`
- Write clear, descriptive commit messages
- Keep commits focused on a single change

## Key Decisions Log

Track important architectural and tooling decisions here as they are made:

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-02-06 | Repository created | Initial project setup |

## Notes for AI Assistants

- This project is in its earliest stage. When making changes, prefer simple, minimal solutions.
- Always read existing files before modifying them.
- Update this CLAUDE.md when adding new tooling, frameworks, directory structure, or conventions.
- If adding a framework or build system, also add the corresponding `.gitignore` entries.
